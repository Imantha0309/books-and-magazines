import React from 'react';
import RegistrationForm from '../shared/RegistrationForm';

const RegisterUser = () => (
  <RegistrationForm
    role="user"
    title="Reader Registration"
    subtitle="Create an account to access the reader dashboard."
  />
);

export default RegisterUser;
